import Button from "@/components/ui/button";

const ContactUsPage = () => {
  return (
    <div className="h-full grid place-items-center min-h-screen">
      <Button size="large" label="رفتن به صفحه تماس با ما" href="/contact" />
    </div>
  );
};

export default ContactUsPage;
